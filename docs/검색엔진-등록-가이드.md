# 육즙관리소 홈페이지 검색 노출 — 순차 진행 가이드

대상 사이트: https://www.yukjeup.com
작성일: 2026-07-27

---

## 0. 현재 상태 (점검 완료)

| 항목 | 상태 |
|---|---|
| sitemap.xml | ✅ 정상 (110개 URL, 22페이지 × 5개 언어) |
| robots.txt | ✅ 정상 (구글·네이버 Yeti·AI봇 전부 허용) |
| 사이트맵 URL 살아있는지 | ✅ 전수 확인, 전부 200 OK |
| non-www → www 리다이렉트 | ✅ 308로 www로 통일됨 |
| canonical 태그 | ✅ 있음 |
| 구조화 데이터(JSON-LD) | ✅ Restaurant / FAQ / Breadcrumb 등 16개 |
| 네이버 소유확인 메타태그 | ✅ 이미 박혀 있음 |
| **구글 소유확인 메타태그** | ❌ **없음 → 아래 1단계에서 처리** |
| GA4 / 네이버 애널리틱스 | ❌ 환경변수 미설정이라 실제로 안 붙음 |

구글에는 이미 일부 색인되어 있음 (`/en` 페이지가 검색에 잡힘).
즉 "안 걸려 있는 게 아니라, 관리 도구에 연결이 안 돼 있는 상태".

---

## 1단계 — 구글 서치콘솔 등록 (제일 먼저)

### 1-1. 접속
https://search.google.com/search-console 에 접속 → 사장님 구글 계정으로 로그인

### 1-2. 속성 추가
왼쪽 위 드롭다운 → **속성 추가**

두 가지 선택지가 나옴:

- **도메인** (왼쪽) — 도메인 등록업체에 DNS TXT 레코드를 넣어야 함. 더 강력하지만 번거로움
- **URL 접두어** (오른쪽) — ✅ **이걸 선택**

**URL 접두어** 칸에 정확히 이렇게 입력:
```
https://www.yukjeup.com
```

### 1-3. 소유권 확인 — "HTML 태그" 방식
확인 방법 목록이 뜨는데 **"HTML 태그"** 를 펼침. 이런 코드가 나옴:

```html
<meta name="google-site-verification" content="여기에긴문자열" />
```

👉 **`content=` 안의 문자열만 복사해서 나(Claude)한테 알려주면**
코드에 넣고 배포까지 해줄게. 배포 끝나면 서치콘솔에서 **확인** 버튼 누르면 됨.

> 직접 하고 싶으면: Vercel 프로젝트 → Settings → Environment Variables →
> `NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION` = (그 문자열) 저장 후 재배포.

### 1-4. 사이트맵 제출
소유권 확인이 끝나면 왼쪽 메뉴 **색인 생성 → Sitemaps**

"새 사이트맵 추가" 칸에 이것만 입력하고 제출:
```
sitemap.xml
```
(앞에 도메인 붙이지 말 것 — 칸에 이미 `https://www.yukjeup.com/` 이 적혀 있음)

몇 분~몇 시간 뒤 상태가 **"성공"**, 검색된 URL **110** 으로 뜨면 정상.

### 1-5. 핵심 페이지 색인 요청 (수동 재촉)
맨 위 검색창(URL 검사)에 아래 주소를 하나씩 넣고,
결과 화면에서 **"색인 생성 요청"** 클릭. 하루 10개 정도가 한도이니 중요한 것부터:

1. `https://www.yukjeup.com/`
2. `https://www.yukjeup.com/menu`
3. `https://www.yukjeup.com/locations/yangjae`
4. `https://www.yukjeup.com/locations/euljiro`
5. `https://www.yukjeup.com/yangjae-matjip`
6. `https://www.yukjeup.com/euljiro-matjip`
7. `https://www.yukjeup.com/dongdaemun-matjip`
8. `https://www.yukjeup.com/faq`

색인은 보통 **2~14일** 걸림. 하루에 계속 확인 안 해도 됨.

---

## 2단계 — 네이버 서치어드바이저 등록

메타태그는 이미 사이트에 박혀 있음
(`naver-site-verification` = `08823a1f...`).
등록을 하다 만 상태일 수 있으니 **먼저 로그인해서 확인**부터.

### 2-1. 접속
https://searchadvisor.naver.com → **웹마스터도구** → 네이버 계정 로그인

### 2-2. 사이트 등록 확인
- 이미 `https://www.yukjeup.com` 이 목록에 있으면 → 2-4로 건너뜀
- 없으면 → 입력창에 `https://www.yukjeup.com` 넣고 등록

### 2-3. 소유확인
"HTML 태그" 방식 선택 → 화면에 나오는 `content` 값이
**`08823a1fd3a76a7be6f8bf049777f51dfe28e9a1` 과 같은지 확인**

- 같으면 → 바로 **소유확인** 버튼 클릭 (이미 사이트에 있으니 통과)
- 다르면 → 새 값을 나한테 알려주면 교체해서 배포해줄게

### 2-4. robots.txt 확인
좌측 **검증 → robots.txt** → "수집 요청" 클릭
`Yeti`(네이버 봇) 허용으로 나오면 정상.

### 2-5. 사이트맵 제출
좌측 **요청 → 사이트맵 제출**

입력:
```
https://www.yukjeup.com/sitemap.xml
```
(네이버는 구글과 달리 **전체 주소**를 넣어야 함)

### 2-6. 웹페이지 수집 요청
좌측 **요청 → 웹페이지 수집**
1단계 1-5의 URL 목록을 하루 몇 개씩 나눠서 넣기. (하루 50개 한도)

### 2-7. 사이트 최적화 점검
좌측 **검증 → 사이트 최적화** 실행 → 빨간 항목 있으면 캡처해서 알려주기.

---

## 3단계 — 네이버는 "플레이스"가 진짜 승부처

네이버 검색에서 음식점은 홈페이지보다 **네이버 플레이스**가 먼저 뜸.
서치어드바이저는 홈페이지용이고, 아래는 별개로 관리해야 함.

- 네이버 플레이스 (양재역점): place ID `1672141709`
- 네이버 플레이스 (더룸 을지로동대문점): place ID `2033717879`

체크할 것:
- [ ] 스마트플레이스 관리자에서 **홈페이지 링크에 `https://www.yukjeup.com` 등록**
- [ ] 상호·주소·전화번호가 홈페이지와 **글자 하나까지 동일**한지 (NAP 일관성)
- [ ] 대표 키워드 등록: `양재역 고기집`, `양재역 삼겹살`, `을지로 맛집`, `동대문 고기집`
- [ ] 사진 30장 이상 / 메뉴판 최신화
- [ ] 리뷰 답글 100% 달기

---

## 4단계 — 측정 도구 붙이기 (선택, 하지만 권장)

지금 GA4·네이버 애널리틱스 스크립트가 **코드에는 있는데 환경변수가 비어서 안 붙고 있음.**
넣으면 "어떤 키워드로 몇 명 들어왔는지"가 보임.

Vercel → Settings → Environment Variables 에 추가 후 재배포:

| 이름 | 값 |
|---|---|
| `NEXT_PUBLIC_GA_ID` | GA4 측정 ID (`G-XXXXXXXXXX`) |
| `NEXT_PUBLIC_NAVER_ACCOUNT_ID` | 네이버 애널리틱스 인증 ID |

GA4 ID는 https://analytics.google.com 에서 속성 만들면 나옴.
네이버 애널리틱스는 https://analytics.naver.com 에서 발급.

---

## 진행 체크리스트

```
[ ] 1-2  구글 서치콘솔 속성 추가 (URL 접두어, www 포함)
[ ] 1-3  구글 소유확인 코드 받아서 Claude에게 전달 → 배포 → 확인
[ ] 1-4  구글에 sitemap.xml 제출
[ ] 1-5  주요 8개 URL 색인 요청
[ ] 2-2  네이버 서치어드바이저 사이트 등록 여부 확인
[ ] 2-3  네이버 소유확인 완료
[ ] 2-5  네이버에 사이트맵 제출 (전체 주소)
[ ] 2-6  네이버 웹페이지 수집 요청
[ ] 3    스마트플레이스 홈페이지 링크·NAP 점검
[ ] 4    GA4 / 네이버 애널리틱스 환경변수 (선택)
```

---

## 이번에 코드에서 고친 것

| 파일 | 내용 |
|---|---|
| `src/app/sitemap.ts` | 사이트맵에 hreflang 대체본 추가 (5개 언어판이 중복 콘텐츠로 안 잡히게) |
| `src/app/sitemap.ts` | `lastmod`를 빌드시각 → 고정 날짜로. 무관한 배포마다 110개 URL이 전부 "수정됨"으로 찍혀 구글이 lastmod를 무시하던 문제 |
| `src/app/robots.ts` | `/admin`, `/login`, `/api/` 크롤 차단 + `Host` 지시자 추가 |
| `src/lib/constants/brand.ts` | 환경변수 누락 시 vercel 미리보기 주소가 새어나가던 폴백 → `https://www.yukjeup.com` |
| `src/app/layout.tsx` | 구글 소유확인 메타태그 자리 마련 (`NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION`) |

> 📌 `sitemap.ts`의 `LAST_MODIFIED` 상수는 **콘텐츠를 실제로 고쳤을 때만** 날짜를 갱신할 것.
